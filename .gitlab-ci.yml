before_script:
  - npm install
  - git remote add heroku https://heroku:$HEROKU_API_KEY@git.heroku.com/fsranking-front.git

deploy:
  only:
    - master
  stage: deploy
  script:
    - npm run build
    - git push -f heroku master
